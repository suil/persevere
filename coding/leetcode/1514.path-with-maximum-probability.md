# Path with Maximum Probability
[1514. Path with Maximum Probability](https://leetcode.com/problems/path-with-maximum-probability/)

You are given an undirected weighted graph of n nodes (0-indexed), represented by an edge list where edges[i] = [a, b] is an undirected edge connecting the nodes a and b with a probability of success of traversing that edge succProb[i].

# Key insights
*

# Code
```javascript
/**
 * @param {number} n
 * @param {number[][]} edges
 * @param {number[]} succProb
 * @param {number} start_node
 * @param {number} end_node
 * @return {number}
 */
var maxProbability = function(n, edges, succProb, start_node, end_node) {
    // // Build adjacency list
    // const graph = new Map();
    // for (let i = 0; i < n; i++) graph.set(i, []);
    // for (let i = 0; i < edges.length; i++) {
    //     const [u, v] = edges[i];
    //     const p = succProb[i];
    //     graph.get(u).push([v, p]);
    //     graph.get(v).push([u, p]);
    // }

    // // Max probability to reach each node
    // const probs = Array(n).fill(0);
    // probs[start_node] = 1;

    // const visited = new Set();

    // // Max-heap priority queue: largest probability first
    // const pq = new PriorityQueue((a, b) => b[0] - a[0]);
    // pq.enqueue([1, start_node]);

    // while (!pq.isEmpty()) {
    //     const [prob, node] = pq.dequeue();

    //     if (visited.has(node)) continue; // skip if already processed
    //     visited.add(node);

    //     if (node === end_node) return prob; // reached destination

    //     for (const [nei, edgeProb] of graph.get(node)) {
    //         if (!visited.has(nei)) {
    //             const newProb = prob * edgeProb;
    //             if (newProb > probs[nei]) {
    //                 probs[nei] = newProb;
    //                 pq.enqueue([newProb, nei]);
    //             }
    //         }
    //     }
    // }

    // return 0; // no path exists

    // Build adjacency list
    const graph = new Map();
    for (let i = 0; i < n; i++) {
        graph.set(i, []);
    }
    for (let i = 0; i < edges.length; i++) {
        const [u, v] = edges[i];
        graph.get(u).push([v, succProb[i]]);
        graph.get(v).push([u, succProb[i]]);
    }

    // Max-heap: higher probability has higher priority
    const pq = new PriorityQueue((a, b) => b.prob - a.prob);
    pq.enqueue({ prob: 1, node: start_node });
    const visited = new Set();

    while (!pq.isEmpty()) {
        const { prob, node } = pq.dequeue();
        if (node === end_node) return prob;
        if (visited.has(node)) continue;
        visited.add(node);

        for (const [nei, edgeProb] of graph.get(node)) {
            if (!visited.has(nei)) {
                pq.enqueue({ prob: prob * edgeProb, node: nei });
            }
        }
    }

    return 0;
};
```

# Complexity
**Time:**
**Space:**
