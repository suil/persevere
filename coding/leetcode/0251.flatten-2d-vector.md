# Flatten 2D Vector
[251. Flatten 2D Vector](https://leetcode.com/problems/flatten-2d-vector/)

Design an iterator to flatten a 2D vector. It should support the next and hasNext operations.

# Key insights
- Filter out empty rows during construction to simplify iteration logic
- Track current row and column indices
- Advance column on each `next()`, move to next row when column reaches end

# Code
```javascript
/**
 * @param {number[][]} vec
 */
var Vector2D = function(vec) {
    this.vec = vec.filter(v => v && v.length > 0);
    this.row = 0;
    this.col = 0;
};

/**
 * @return {number}
 */
Vector2D.prototype.next = function() {
    if (!this.hasNext()) return null;

    const val = this.vec[this.row][this.col++];
    if (this.col === this.vec[this.row].length) {
        this.row++;
        this.col = 0;
    }
    return val;
};

/**
 * @return {boolean}
 */
Vector2D.prototype.hasNext = function() {
    return this.row < this.vec.length && this.col < this.vec[this.row].length;
};

/** 
 * Your Vector2D object will be instantiated and called as such:
 * var obj = new Vector2D(vec)
 * var param_1 = obj.next()
 * var param_2 = obj.hasNext()
 */
```

# Complexity
**Time:** O(v) for constructor where v is number of vectors; O(1) for next() and hasNext()
**Space:** O(1) extra space - only storing row and column indices
