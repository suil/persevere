# Is Subsequence
[392. Is Subsequence](https://leetcode.com/problems/is-subsequence/)

Given two strings s and t, return true if s is a subsequence of t, or false otherwise.

# Key insights
*

# Code
```javascript
/**
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
var isSubsequence = function(s, t) {
  if (s.length === 0) {
    return true;
  }

  /* two pointer */

//   let sp = 0;
//   let tp = 0;

//   while (sp < s.length && tp < t.length) {
//     if (s[sp] === t[tp]) {
//       sp++;
//     }
//     tp++;
//   }

//   return sp === s.length;

    // function backtrack(sIdx, tIdx, memo = {}) {
    //     const memoKey = `${sIdx}-${tIdx}`;
    //     if (memoKey in memo) return memo[memoKey];

    //     if (sIdx >= s.length) return true;
    //     if (tIdx >= t.length) return false;

    //     if (s[sIdx] === t[tIdx]) {
    //         memo[memoKey] = backtrack(sIdx + 1, tIdx + 1, memo);
    //     } else {
    //         memo[memoKey] = backtrack(sIdx, tIdx + 1, memo);
    //     }

    //     return memo[memoKey];
    // }

    // return backtrack(0, 0);

    const dp = Array(s.length + 1).fill().map(() => Array(t.length + 1).fill(false));
    for (let i = 0; i <= t.length; i++) dp[0][i] = true;

    for (let si = 1; si <= s.length; si++) {
        for (let ti = 1; ti <= t.length; ti++) {
            if (s[si - 1] === t[ti - 1]) {
                dp[si][ti] = dp[si - 1][ti - 1];
            } else {
                dp[si][ti] = dp[si][ti - 1];
            }
        }
    }
    return dp[s.length][t.length];
};
```

# Complexity
**Time:**
**Space:**
