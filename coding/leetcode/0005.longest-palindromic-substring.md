# Longest Palindromic Substring
[5. Longest Palindromic Substring](https://leetcode.com/problems/longest-palindromic-substring)

Given a string s, return the longest palindromic substring in s.

# Key insights
* Expand around center approach - check palindromes by growing outward from each position
* Handle both odd and even length palindromes by using different center configurations
* Two pointers technique expanding left and right simultaneously until mismatch found
* O(n²) time complexity with O(1) space - more efficient than dynamic programming approach

# Code
```javascript
var longestPalindrome = function(s) {
  let strlen = s.length;
  let maxLen = 0;
  let result = '';

  for (let center = 0; center < strlen; center++) {
    // odd length
    let start = center;
    let end = center;
    while (start >= 0 && end < strlen) {
      if (s[start] !== s[end]) { break; }

      const len = end - start + 1;
      if (len > maxLen) {
        maxLen = len;
        result = s.substring(start, end + 1);
      }
      start--;
      end++;
    }

    // even length
    start = center;
    end = center + 1;
    while (start >= 0 && end < strlen) {
      if (s[start] !== s[end]) { break; }

      const len = end - start + 1;
      if (len > maxLen) {
        maxLen = len;
        result = s.substring(start, end + 1);
      }
      start--;
      end++;
    }
  }

  return result;
};
```

# Complexity
**Time:** O(n²) - For each of the n possible centers, we expand outward up to n/2 positions in the worst case
**Space:** O(1) - Only uses a constant amount of extra variables regardless of input size
