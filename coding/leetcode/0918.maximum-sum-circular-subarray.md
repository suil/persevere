# Maximum Sum Circular Subarray
[918. Maximum Sum Circular Subarray](https://leetcode.com/problems/maximum-sum-circular-subarray/)

Given a circular integer array nums of length n, return the maximum possible sum of a non-empty subarray of nums.

# Key insights
- Two cases: max subarray is contiguous (normal Kadane's) or wraps around the ends
- Wrap-around case: totalSum - minSubarray = sum of elements outside the minimum subarray
- Use Kadane's for both max and min subarray simultaneously
- Edge case: if all elements are negative, minSum equals totalSum, so return maxSum

# Code
```javascript
/**
 * @param {number[]} nums
 * @return {number}
 */
var maxSubarraySumCircular = function(nums) {
    let curMax = 0;
    let curMin = 0;
    let maxSum = nums[0];
    let minSum = nums[0];
    let totalSum = 0;
    
    for (const num of nums) {
        // Normal Kadane's
        curMax = Math.max(curMax, 0) + num;
        maxSum = Math.max(maxSum, curMax);
        
        // Kadane's but with min to find minimum subarray
        curMin = Math.min(curMin, 0) + num;
        minSum = Math.min(minSum, curMin);
        
        totalSum += num;  
    }

    if (totalSum === minSum) {
        return maxSum;
    }
    
    return Math.max(maxSum, totalSum - minSum);
};
```

# Complexity
**Time:** O(n) - single pass through the array
**Space:** O(1) - only using a few variables
